import type { FC } from "react";
import Sidebar from "../Containers/Sidebar";

type WithSidebarProps = {
    //
};

const WithSidebar: FC<WithSidebarProps> = ({}) => <Sidebar />;

export default WithSidebar;
